<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="Create a new user — My Recipe Web" />
  <title>New User — My Recipe Web</title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <a href="#main-content" class="skip-link">Skip to main content</a>

  <header>
    <div class="container">
      <h1 class="site-title">My Recipe Web</h1>
      <nav aria-label="Main navigation">
        <ul class="nav">
          <li><a href="/home">Home</a></li>
          <li><a href="/about">About</a></li>
          <li><a href="/subscribers">Subscribers</a></li>
          <li><a href="/users" aria-current="page">Users</a></li>
          <li><a href="/courses">Courses</a></li>
          <li><a href="/contact">Contact</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main id="main-content">
    <section class="hero">
      <div class="container">
        <h2>Create a New User</h2>
        <p>Provide first/last name, a valid email, and a password (min 6 chars).</p>
      </div>
    </section>

    <section>
      <div class="container">
        <div class="card">
          <form action="/users" method="POST" novalidate>
            <div>
              <label for="first">First Name</label><br />
              <input id="first" name="first" type="text" value="<%= values?.first || '' %>" required />
              <% if (errors?.first) { %><small><%= errors.first %></small><% } %>
            </div>

            <div>
              <label for="last">Last Name</label><br />
              <input id="last" name="last" type="text" value="<%= values?.last || '' %>" required />
              <% if (errors?.last) { %><small><%= errors.last %></small><% } %>
            </div>

            <div>
              <label for="email">Email</label><br />
              <input id="email" name="email" type="email" value="<%= values?.email || '' %>" required />
              <% if (errors?.email) { %><small><%= errors.email %></small><% } %>
            </div>

            <div>
              <label for="zipCode">Zip Code (optional)</label><br />
              <input id="zipCode" name="zipCode" type="text" value="<%= values?.zipCode || '' %>" />
              <% if (errors?.zipCode) { %><small><%= errors.zipCode %></small><% } %>
            </div>

            <div>
              <label for="password">Password</label><br />
              <input id="password" name="password" type="password" value="" required />
              <% if (errors?.password) { %><small><%= errors.password %></small><% } %>
            </div>

            <% if (errors?._general) { %><p><small><%= errors._general %></small></p><% } %>

            <div>
              <button type="submit" class="btn">Create User</button>
              <a href="/users" class="btn">Cancel</a>
            </div>
          </form>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">
      <p>&copy; 2025 My Recipe Web. Cook, Learn, and Share with Us.</p>
    </div>
  </footer>
</body>
</html>
