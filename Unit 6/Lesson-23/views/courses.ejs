<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="Browse cooking courses — My Recipe Web" />
  <title>Courses — My Recipe Web</title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <a href="#main-content" class="skip-link">Skip to main content</a>

  <header>
    <div class="container">
      <h1 class="site-title">My Recipe Web</h1>
      <nav aria-label="Main navigation">
        <ul class="nav">
          <li><a href="/home">Home</a></li>
          <li><a href="/about">About</a></li>
          <li><a href="/subscribers">Subscribers</a></li>
          <li><a href="/users">Users</a></li>
          <li><a href="/courses" aria-current="page">Courses</a></li>
          <li><a href="/contact">Contact</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main id="main-content">
    <section class="hero" aria-labelledby="courses-heading">
      <div class="container">
        <h2 id="courses-heading">Courses</h2>
        <p>Manage your cooking courses: create, edit, and remove.</p>
        <a href="/courses/new" class="btn">Add New Course</a>
      </div>
    </section>

    <section>
      <div class="container">
        <div class="page-header">
          <h2 class="section-title">Course List</h2>
        </div>

        <% if (courses && courses.length) { %>
          <ul class="subscriber-list">
            <% courses.forEach((c, i) => { %>
              <li class="subscriber-item">
                <div class="subscriber-info">
                  <strong><%= c.title %></strong>
                  <p><%= c.description %></p>
                  <small>Zip: <%= c.zipCode || "-" %></small>
                  <small>
                    Items:
                    <% if (c.items && c.items.length) { %>
                      <%= c.items.join(", ") %>
                    <% } else { %>
                      -
                    <% } %>
                  </small>
                </div>

                <div class="card" role="group" aria-label="Actions for <%= c.title %>">
                  <a href="/courses/<%= c._id %>/edit" class="btn">Edit</a>
                  <form action="/courses/<%= c._id %>/delete" method="POST">
                    <button type="submit" class="btn" onclick="return confirm('Delete this course?')">Delete</button>
                  </form>
                </div>
              </li>
            <% }) %>
          </ul>
        <% } else { %>
          <div class="empty-state">
            <p>No courses yet.</p>
            <a href="/courses/new" class="btn">Create the first course</a>
          </div>
        <% } %>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">
      <p>&copy; 2025 My Recipe Web. Cook, Learn, and Share with Us.</p>
    </div>
  </footer>
</body>
</html>
