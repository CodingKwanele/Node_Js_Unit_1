<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Join our community of food lovers and cooking enthusiasts.">
    <title>Subscribers — My Recipe Web</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <!-- PUBLIC VERSION: No email addresses shown -->

    <a href="#main-content" class="skip-link">Skip to main content</a>

    <header>
        <div class="container">
            <h1 class="site-title">My Recipe Web</h1>
            <nav aria-label="Main navigation">
                <ul class="nav">
                    <li><a href="/home">Home</a></li>
                    <li><a href="/about">About</a></li>
                    <li><a href="/subscribers" aria-current="page">Subscribers</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main id="main-content">
        <section class="hero">
            <div class="container">
                <h2>Join Our Community</h2>
                <p>Subscribe to get recipes, cooking tips, and updates from My Recipe Web.</p>
                <a href="/contact" class="btn">Subscribe Now</a>
            </div>
        </section>

        <section>
            <div class="container">
                <% if (!subscribers || subscribers.length === 0) { %>
                    <p>Be the first to join our growing community of food lovers!</p>
                <% } else { %>
                    <div class="community-stats">
                        <p><strong><%= subscribers.length %></strong> <%= subscribers.length === 1 ? 'person has' : 'people have' %> joined our community</p>
                    </div>
                    
                    <div class="recent-members">
                        <h3>Recent Members</h3>
                        <ul class="member-list">
                            <% subscribers.slice(0, 10).forEach(s => { %>
                                <li class="member-item">
                                    <strong><%= s.name %></strong>
                                    <% if (s.createdAt) { %>
                                        <small>Joined <%= new Date(s.createdAt).toLocaleDateString() %></small>
                                    <% } %>
                                </li>
                            <% }) %>
                        </ul>
                    </div>
                <% } %>
            </div>
        </section>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2025 My Recipe Web. Cook, Learn, and Share with Us.</p>
        </div>
    </footer>
</body>
</html>