<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="Log in — My Recipe Web" />
  <title>Login — My Recipe Web</title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <a href="#main-content" class="skip-link">Skip to main content</a>

  <header>
    <div class="container">
      <h1 class="site-title">My Recipe Web</h1>
      <nav aria-label="Main navigation">
        <ul class="nav">
          <li><a href="/home">Home</a></li>
          <li><a href="/about">About</a></li>
          <li><a href="/subscribers">Subscribers</a></li>
          <li><a href="/users">Users</a></li>
          <li><a href="/courses">Courses</a></li>
          <li><a href="/contact">Contact</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main id="main-content">
    <section class="hero">
      <div class="container">
        <h2>Login</h2>
        <p>Welcome back. Please sign in.</p>
      </div>
    </section>

    <!-- flash messages -->
    <section aria-live="polite" aria-atomic="true">
      <div class="container">
        <% if (flashMessages && flashMessages.error) { %>
          <div class="alert alert-danger" role="alert">
            <%= flashMessages.error %>
          </div>
        <% } %>

        <% if (flashMessages && flashMessages.success) { %>
          <div class="alert alert-success" role="alert">
            <%= flashMessages.success %>
          </div>
        <% } %>

        <% if (session && session.userName) { %>
        <p class="logged-in-banner">Signed in as <strong><%= session.userName %></strong></p>
        <% } %>

      </div>
    </section>

    <section>
      <div class="container">
        <div class="card">
          <form action="/users/login" method="POST" novalidate>
            <div>
              <label for="inputEmail">Email address</label><br />
              <input
                type="email"
                name="email"
                id="inputEmail"
                placeholder="Email address"
                required
              />
            </div>

            <div>
              <label for="inputPassword">Password</label><br />
              <input
                type="password"
                name="password"
                id="inputPassword"
                placeholder="Password"
                required
              />
            </div>

            <div style="margin-top: 1rem;">
              <button type="submit" class="btn">Login</button>
              <a href="/users/new" class="btn">Sign up</a>
            </div>
          </form>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">
      <p>&copy; 2025 My Recipe Web. Cook, Learn, and Share with Us.</p>
    </div>
  </footer>
</body>
</html>
