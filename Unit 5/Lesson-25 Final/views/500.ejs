<section>
  <div class="container" style="max-width: 560px;">
    <div class="card" role="alert" style="text-align:center; padding:2rem;">
      <h2 class="section-title" style="margin-bottom:0.5rem;">500 — Server Error</h2>
      <p><%= typeof message === "string" ? message : "Something went wrong. Please try again later." %></p>
      <% if (stack) { %>
        <pre style="text-align:left; overflow:auto; max-height:240px;"><%= stack %></pre>
      <% } %>
      <a href="/home" class="btn">Return to Home</a>
    </div>
  </div>
</section>
