<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="Edit course — My Recipe Web">
  <title>Edit Course — My Recipe Web</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <a href="#main-content" class="skip-link">Skip to main content</a>

    <header>
        <div class="container">
            <h1 class="site-title">My Recipe Web</h1>
            <nav aria-label="Main navigation">
                <ul class="nav">
                    <li><a href="/home">Home</a></li>
                    <li><a href="/about" aria-current="page">About</a></li>
                    <li><a href="/subscribers">Subscribers</a></li>
                    <li><a href="/users">Users</a></li>         
                    <li><a href="/courses">Courses</a></li>     
                    <li><a href="/contact">Contact</a></li>     <!-- optional: quick subscribe link -->
                </ul>
            </nav>
        </div>
    </header>

  <main id="main-content">
    <section class="hero">
      <div class="container">
        <h2>Edit Course</h2>
        <p>Update the fields below and save your changes.</p>
      </div>
    </section>

    <section>
      <div class="container">
        <div class="card lift">
          <form action="/courses/<%= id %>" method="POST" novalidate>
            <div>
              <label for="title">Title</label><br>
              <input id="title" name="title" type="text" value="<%= values?.title || '' %>" required aria-describedby="title-error">
              <% if (errors?.title) { %><div id="title-error"><small><%= errors.title %></small></div><% } %>
            </div>

            <div>
              <label for="description">Description</label><br>
              <textarea id="description" name="description" rows="4" required aria-describedby="description-error"><%= values?.description || '' %></textarea>
              <% if (errors?.description) { %><div id="description-error"><small><%= errors.description %></small></div><% } %>
            </div>

            <div>
              <label for="zipCode">Zip Code (optional)</label><br>
              <input id="zipCode" name="zipCode" type="text" value="<%= values?.zipCode || '' %>" aria-describedby="zip-error">
              <% if (errors?.zipCode) { %><div id="zip-error"><small><%= errors.zipCode %></small></div><% } %>
            </div>

            <div>
              <label for="items">Items (comma-separated)</label><br>
              <input id="items" name="items" type="text" value="<%= values?.items || '' %>">
            </div>

            <% if (errors?._general) { %>
              <div><small><%= errors._general %></small></div>
            <% } %>

            <div style="margin-top:1rem;">
              <button type="submit" class="btn">Save Changes</button>
              <a href="/courses" class="btn">Cancel</a>
              <form action="/courses/<%= id %>/delete" method="POST" style="display:inline">
                <button type="submit" class="btn">Delete</button>
              </form>
            </div>
          </form>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">
      <p>&copy; 2025 My Recipe Web. Cook, Learn, and Share with Us.</p>
    </div>
  </footer>
</body>
</html>
